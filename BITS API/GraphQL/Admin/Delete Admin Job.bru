meta {
  name: Delete Admin Job
  type: http
  seq: 7
}

post {
  url: http://localhost:8080/graphql
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {
    "query": "mutation DeleteAdminJob($id: ID!) { deleteAdminJob(id: $id) { message } }",
    "variables": {
      "id": "1"
    }
  }
}
